import styled from '@emotion/styled';

const StyledFigure = styled('figure')`
  cursor: pointer;
  display: flex;
  padding: 0;
  margin: 0;
  height: 100%;
  align-self: center;
`;

export default StyledFigure;
